<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Blog</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="overflow-hidden">
  <header>
    <h1>Welcome to My Blog</h1>
    <a href="/form" class="create-button">Create New Post</a>
  </header>

  <section class="blog-posts overflow-y-auto">

    <% if (data.length> 0) { %>
      <% data.forEach((element)=> {%>
        <article class="post">
          <h2 class=" font-semibold capitalize">
            <%= element %>
          </h2>
          <div class="flex justify-between">
            <a href="/<%= element %>" class="text-blue-500">Read more..</a>
            <a href="/edit/<%= element %>" class="text-yellow-500">Edit</a>
          </div>
        </article>
        <% }); %>
          <% } else { %>
            <div class="errorText flex justify-center items-center text-3xl font-semibold">You have no blogs post at
              this moment
            </div>
            <% } %>

  </section>
</body>

</html>